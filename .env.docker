# ============================================
# Docker Build Performance - Maximum Speed
# ============================================

# Enable BuildKit (required for cache mounts)
# Supported on: Docker 18.09+
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# Enable Bake for parallel builds (optional)
# Requires: docker-buildx plugin
# Benefits: Faster multi-service builds, better dependency handling
# Note: Auto-detected by ./docker-build.sh
# COMPOSE_BAKE=true

# BuildKit Configuration
# - auto: Clean, minimal output (recommended)
# - plain: Detailed output (for debugging)
BUILDKIT_PROGRESS=auto
